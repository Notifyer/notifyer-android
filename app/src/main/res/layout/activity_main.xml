<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/startServiceButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="50dp"
        android:text="Start Service"
        app:layout_constraintBottom_toTopOf="@+id/stoptServiceButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:fontFamily="@font/courier"/>

    <Button
        android:id="@+id/stoptServiceButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="100dp"
        android:text="Stop Service"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        android:fontFamily="@font/courier"/>

    <EditText
        android:id="@+id/notifyerCode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="50dp"
        android:ems="10"
        android:hint="Notifyer Code"
        android:inputType="text"
        android:minHeight="48dp"
        app:layout_constraintBottom_toTopOf="@+id/startServiceButton"
        app:layout_constraintEnd_toEndOf="@+id/startServiceButton"
        app:layout_constraintStart_toStartOf="@+id/startServiceButton"
        android:fontFamily="@font/courier"/>

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="150dp"
        android:text="Notifyer"
        android:textSize="40sp"
        app:layout_constraintBottom_toTopOf="@+id/notifyerCode"
        app:layout_constraintEnd_toEndOf="@+id/startServiceButton"
        app:layout_constraintStart_toStartOf="@+id/startServiceButton"
        android:fontFamily="@font/courier"/>
</androidx.constraintlayout.widget.ConstraintLayout>